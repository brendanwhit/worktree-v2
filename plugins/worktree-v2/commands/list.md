List all active worktree entries from the global registry.

Run the worktree CLI list command:

```bash
python scripts/cli/worktree.py list $ARGUMENTS
```

Shows each worktree's name, repo, branch, path, and sandbox name (if any).

## Examples

```
/worktree:list
```
