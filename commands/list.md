List all active entries from the global registry.

Run the superintendent CLI list command:

```bash
superintendent list $ARGUMENTS
```

Shows each entry's name, repo, branch, path, and sandbox name (if any).

## Examples

```
/superintendent:list
```
